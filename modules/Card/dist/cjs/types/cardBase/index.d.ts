import React from 'react';
import './styles.css';
import { CardProps } from '../interfaces';
export declare const Card: ({ children, id, customClassName, borderAccent, paddingSize, shadow }: CardProps) => React.JSX.Element;
