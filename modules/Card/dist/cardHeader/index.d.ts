import React from 'react';
import './styles.css';
interface CardHeader {
    children?: React.ReactNode;
    title: string;
}
export declare const CardHeader: ({ children, title }: CardHeader) => React.JSX.Element;
export {};
