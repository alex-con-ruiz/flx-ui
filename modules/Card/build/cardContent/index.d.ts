import React from 'react';
import './styles.css';
interface CardContent {
    children: React.ReactNode;
}
export declare const CardContent: ({ children }: CardContent) => React.JSX.Element;
export {};
